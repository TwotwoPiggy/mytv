---
phase: 01-verification-bypass-playback-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/horsenma/yourtv/UserVerificationHandler.kt
  - app/src/main/java/com/horsenma/yourtv/UserInfo.kt
  - app/src/main/java/com/horsenma/yourtv/MainActivity.kt
  - app/src/main/java/com/horsenma/yourtv/SettingFragment.kt
  - app/src/main/java/com/horsenma/yourtv/MenuFragment.kt
  - app/src/main/java/com/horsenma/yourtv/MainViewModel.kt
  - app/src/main/res/layout/setting.xml
  - app/src/main/res/layout/setting_mytv1.xml
  - app/src/main/res/layout/userconfirm.xml
autonomous: true
requirements:
  - LIMITS-01
  - LIMITS-02
user_setup: []

must_haves:
  truths:
    - "App boots without showing any verification/activation dialog"
    - "No network requests to D1 database for user status or device binding during boot"
    - "Channel playback starts automatically after loading completes"
    - "Source file import via LAN admin continues to work"
    - "UserInfoManager.apiKeys remains available for DownGithubPrivate.download()"
  artifacts:
    - path: "app/src/main/java/com/horsenma/yourtv/UserInfo.kt"
      provides: "Simplified UserInfoManager with apiKeys and loadApiKeys preserved"
      contains: "fun loadApiKeys()"
    - path: "app/src/main/java/com/horsenma/yourtv/MainActivity.kt"
      provides: "Clean onCreate without verification initialization"
      contains: "UserInfoManager.initialize()"
  key_links:
    - from: "MainActivity.kt"
      to: "UserInfoManager.initialize()"
      via: "simplified init call"
      pattern: "UserInfoManager\\.initialize\\(\\)"
    - from: "DownGithubPrivate.kt"
      to: "UserInfoManager.apiKeys"
      via: "API key access for Worker downloads"
      pattern: "UserInfoManager\\.apiKeys"
---

<objective>
Remove all verification, activation, and test-code logic from the application so it boots directly to playback without any dialogs or network checks.

Purpose: Eliminate the verification barrier that prevents users from using the app immediately. This is the foundational "removal" workstream of Phase 1.
Output: Cleaned codebase with no verification code paths; app boots to LoadingFragment then auto-plays.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verification-bypass-playback-architecture/01-CONTEXT.md
@.planning/phases/01-verification-bypass-playback-architecture/01-RESEARCH.md
@.planning/phases/01-verification-bypass-playback-architecture/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surgically Remove All Verification Code</name>
  <files>
    app/src/main/java/com/horsenma/yourtv/UserVerificationHandler.kt,
    app/src/main/java/com/horsenma/yourtv/UserInfo.kt,
    app/src/main/java/com/horsenma/yourtv/MainActivity.kt,
    app/src/main/java/com/horsenma/yourtv/SettingFragment.kt,
    app/src/main/java/com/horsenma/yourtv/MenuFragment.kt,
    app/src/main/java/com/horsenma/yourtv/MainViewModel.kt,
    app/src/main/res/layout/setting.xml,
    app/src/main/res/layout/setting_mytv1.xml,
    app/src/main/res/layout/userconfirm.xml
  </files>
  <read_first>
    app/src/main/java/com/horsenma/yourtv/UserVerificationHandler.kt,
    app/src/main/java/com/horsenma/yourtv/UserInfo.kt,
    app/src/main/java/com/horsenma/yourtv/MainActivity.kt,
    app/src/main/java/com/horsenma/yourtv/SettingFragment.kt,
    app/src/main/java/com/horsenma/yourtv/MenuFragment.kt,
    app/src/main/java/com/horsenma/yourtv/MainViewModel.kt,
    app/src/main/java/com/horsenma/yourtv/DownGithubPrivate.kt,
    app/src/main/res/layout/setting.xml,
    app/src/main/res/layout/setting_mytv1.xml,
    app/src/main/res/layout/userconfirm.xml
  </read_first>
  <action>
    **DELETE entire files (per D-01):**
    - Delete `app/src/main/java/com/horsenma/yourtv/UserVerificationHandler.kt`
    - Delete `app/src/main/res/layout/userconfirm.xml`

    **MODIFY UserInfo.kt (per D-01, D-05) — CRITICAL: preserve apiKeys:**
    - Remove methods: `validateKey()`, `checkBinding()`, `updateBinding()`, `checkExpiredTestCodes()`, `getTestCodes()`, `saveTestCode()`, `getActiveUserId()`
    - Remove constants: `KEY_TEST_CODES`, `KEY_ACTIVE_USER_ID`, `KEY_LAST_CHECK_TIME`
    - In `initialize()`: remove the `checkExpiredTestCodes` coroutine block (lines 115-129 per RESEARCH.md). Keep only `loadApiKeys()` call and `loadCache()` call
    - PRESERVE: `UserInfoManager` object declaration, `apiKeys` field, `loadApiKeys()` method, `loadCache()` method, `initialize()` method signature

    **MODIFY MainActivity.kt (per D-01, D-04):**
    - Remove field: `userVerificationHandler` (line 128 per PATTERNS.md)
    - Remove field: `dialog` (line 129)
    - Remove interface: `VerificationCallback` (lines 135-139)
    - Remove init block: `verificationCallback` init (lines 155-169)
    - Remove field: `isLoadingInputVisible` (line 119)
    - Remove method: `setLoadingInputVisible()` (lines 124-126)
    - In `onKey()`: remove verification-related key handling blocks at lines 1253-1256, 1268-1271, 1285-1306, 1336-1348, 1357-1370
    - Keep: `UserInfoManager.initialize()` call in `onCreate()` — this will now only load apiKeys

    **MODIFY SettingFragment.kt (per D-02):**
    - Remove: `binding.verifyUser.setOnClickListener` (lines 319-321)
    - Remove: `showVerificationDialog()` method (lines 678-736)
    - Remove: `binding.verifyUser` references in layout iteration (line 354)

    **MODIFY MenuFragment.kt (per D-04):**
    - Remove: `test_code_expired` broadcast receiver registration and handler (lines 159-181)

    **MODIFY MainViewModel.kt (per D-04):**
    - Remove: `deleteCacheByTestCode()` method (lines 1120-1153)

    **MODIFY setting.xml (per D-02):**
    - Remove: `verify_user` button element (lines 151-163)
    - Remove: `nextFocusDown/Right/Left` attributes that reference `verify_user` (lines 110, 145, 174) — set these to point to the next valid focus target instead

    **MODIFY setting_mytv1.xml (per D-02):**
    - Same changes as setting.xml: remove `verify_user` button and fix focus references
  </action>
  <verify>
    <automated>cd D:/Computers/AIDevelop/Tools/mytv && rtk ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - UserVerificationHandler.kt does not exist in the source tree
    - userconfirm.xml does not exist in the res/layout directory
    - UserInfo.kt still contains `fun loadApiKeys()` and `apiKeys` field
    - UserInfo.kt does NOT contain `fun validateKey(`, `fun checkBinding(`, `fun checkExpiredTestCodes(`, `fun getTestCodes(`, `fun saveTestCode(`, `fun getActiveUserId(`
    - UserInfo.kt `initialize()` does NOT contain `checkExpiredTestCodes` (verified via grep)
    - MainActivity.kt does NOT contain `userVerificationHandler`, `VerificationCallback`, `isLoadingInputVisible`, `setLoadingInputVisible`
    - SettingFragment.kt does NOT contain `verifyUser`, `showVerificationDialog`
    - MenuFragment.kt does NOT contain `test_code_expired`
    - MainViewModel.kt does NOT contain `deleteCacheByTestCode`
    - setting.xml does NOT contain `verify_user`
    - setting_mytv1.xml does NOT contain `verify_user`
    - DownGithubPrivate.kt still references `UserInfoManager.apiKeys` (unchanged)
    - Project compiles successfully (assembleDebug exits 0)
  </acceptance_criteria>
  <done>All verification code removed; apiKeys dependency preserved; project compiles</done>
</task>

<task type="auto">
  <name>Task 2: Verify Boot-to-Play Flow Without Verification</name>
  <files>
    app/src/main/java/com/horsenma/yourtv/MainActivity.kt,
    app/src/main/java/com/horsenma/yourtv/LoadingFragment.kt
  </files>
  <read_first>
    app/src/main/java/com/horsenma/yourtv/MainActivity.kt,
    app/src/main/java/com/horsenma/yourtv/LoadingFragment.kt,
    app/src/main/java/com/horsenma/yourtv/MainViewModel.kt
  </read_first>
  <action>
    **Verify boot flow (per D-12, D-13, D-14):**

    Read `MainActivity.onCreate()` and confirm the flow is:
    1. `UserInfoManager.initialize()` is called (now only loads apiKeys, no verification)
    2. Fragment initialization: `LoadingFragment` is shown
    3. `viewModel.init()` runs with timeout
    4. Stable source check: tries to resume last channel (per D-13)
    5. Channel loading and playback trigger

    Confirm no verification dialog is shown at any point in the boot sequence.

    **If any verification remnants found in onCreate() boot flow:** Remove them. The boot sequence should go directly from LoadingFragment to playback.

    **Confirm LoadingFragment behavior (per D-12):**
    - LoadingFragment shows a loading bar
    - Calls `activity.ready()` on creation
    - When hidden (data loaded), calls `stopMusicAndSwitchToLive()` which triggers ChannelFragment playback
    - LoadingFragment removes itself after completion

    **Confirm no-source handling (per D-14):**
    - Read the source loading logic in MainViewModel
    - Confirm that when no sources are loaded, the app shows a friendly prompt (the existing behavior should already handle this via ErrorFragment or a toast)
    - If no graceful handling exists, add a check: if `viewModel.channels` is empty after init, show a message directing user to LAN admin at `http://<device-ip>:8080`
  </action>
  <verify>
    <automated>cd D:/Computers/AIDevelop/Tools/mytv && rtk ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `MainActivity.onCreate()` calls `UserInfoManager.initialize()` but no verification dialog is triggered
    - `LoadingFragment` is shown during boot and transitions to playback
    - No reference to `UserVerificationHandler`, `showVerificationDialog`, or `VerificationCallback` remains in the boot path
    - assembleDebug exits 0 (project compiles)
  </acceptance_criteria>
  <done>Boot flow goes LoadingFragment -> data load -> auto-play with no verification gate</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device-local -> LAN network | SimpleServer receives source files from LAN browsers |
| Device-local -> Internet | DownGithubPrivate downloads sources via Worker API using apiKeys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | SharedPreferences cleanup | accept | Old verification keys remain on disk but are dead code; no security impact |
| T-01-02 | Information Disclosure | apiKeys in memory | accept | apiKeys are already in memory in current code; no change in exposure |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this phase |
</threat_model>

<verification>
1. Build verification: `./gradlew assembleDebug` exits 0
2. Code verification: `grep -r "UserVerificationHandler\|VerificationCallback\|showVerificationDialog\|test_code_expired\|deleteCacheByTestCode\|KEY_TEST_CODES\|KEY_ACTIVE_USER_ID" app/src/main/java/` returns no matches
3. apiKeys preservation: `grep -n "apiKeys\|loadApiKeys" app/src/main/java/com/horsenma/yourtv/UserInfo.kt` shows apiKeys field and loadApiKeys method still present
4. DownGithubPrivate dependency: `grep -n "UserInfoManager.apiKeys" app/src/main/java/com/horsenma/yourtv/DownGithubPrivate.kt` still matches
</verification>

<success_criteria>
- Zero verification code paths remain in the codebase
- UserInfoManager.apiKeys and loadApiKeys() preserved for DownGithubPrivate.download()
- App boot flow: LoadingFragment -> data load -> auto-play with no verification dialog
- Project compiles successfully
</success_criteria>

<output>
Create `.planning/phases/01-verification-bypass-playback-architecture/01-01-SUMMARY.md` when done
</output>
