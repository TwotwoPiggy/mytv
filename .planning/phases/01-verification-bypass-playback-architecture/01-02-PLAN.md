---
phase: 01-verification-bypass-playback-architecture
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/src/main/java/com/horsenma/yourtv/ExoPlayerCallback.kt
  - app/src/main/java/com/horsenma/yourtv/ExoPlayerEngine.kt
  - app/src/main/java/com/horsenma/yourtv/PlayerFragment.kt
autonomous: true
requirements:
  - PERF-02
user_setup: []

must_haves:
  truths:
    - "ExoPlayerEngine encapsulates all ExoPlayer creation, playback, monitoring, and release"
    - "ExoPlayerCallback interface notifies PlayerFragment of playback state changes"
    - "PlayerFragment delegates all ExoPlayer operations to ExoPlayerEngine"
    - "Engine switching (ExoPlayer <-> WebView) immediately destroys the previous engine"
    - "No ExoPlayer references remain directly in PlayerFragment (only via ExoPlayerEngine)"
    - "Handler callbacks are cleared on engine release to prevent leaks"
  artifacts:
    - path: "app/src/main/java/com/horsenma/yourtv/ExoPlayerCallback.kt"
      provides: "Callback interface for ExoPlayerEngine -> PlayerFragment communication"
      contains: "interface ExoPlayerCallback"
    - path: "app/src/main/java/com/horsenma/yourtv/ExoPlayerEngine.kt"
      provides: "Encapsulated ExoPlayer lifecycle management"
      contains: "class ExoPlayerEngine"
      min_lines: 200
    - path: "app/src/main/java/com/horsenma/yourtv/PlayerFragment.kt"
      provides: "Controller that orchestrates ExoPlayerEngine and WebFragment"
      contains: "exoPlayerEngine"
  key_links:
    - from: "PlayerFragment.kt"
      to: "ExoPlayerEngine.kt"
      via: "ExoPlayerEngine instance field"
      pattern: "exoPlayerEngine"
    - from: "ExoPlayerEngine.kt"
      to: "ExoPlayerCallback.kt"
      via: "callback interface"
      pattern: "callback\\?\\."
    - from: "PlayerFragment.kt"
      to: "ExoPlayerCallback.kt"
      via: "implements callback to receive events"
      pattern: "ExoPlayerCallback"
---

<objective>
Extract ExoPlayer logic from PlayerFragment into a dedicated ExoPlayerEngine class with callback-based communication, enabling clean engine switching and preventing memory leaks.

Purpose: Separate playback engine concerns from UI controller concerns. This is the "architecture" workstream of Phase 1.
Output: ExoPlayerEngine class, ExoPlayerCallback interface, refactored PlayerFragment that delegates to engine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verification-bypass-playback-architecture/01-CONTEXT.md
@.planning/phases/01-verification-bypass-playback-architecture/01-RESEARCH.md
@.planning/phases/01-verification-bypass-playback-architecture/01-PATTERNS.md
@.planning/phases/01-verification-bypass-playback-architecture/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From app/src/main/java/com/horsenma/mytv1/WebFragmentCallback.kt:
```kotlin
package com.horsenma.mytv1

interface WebFragmentCallback {
    fun onPlaybackStarted()
    fun onPlaybackStopped()
    fun onPlaybackError(error: String)
}
```

From app/src/main/java/com/horsenma/yourtv/PlayerFragment.kt (key fields):
```kotlin
private var player: ExoPlayer? = null
private val handler = Handler(Looper.myLooper()!!)
private var tvModel: TVModel? = null
private var isStable = false
private var playbackStartTime = 0L
private var lastStopTime = 0L
private var bufferingCount = 0
```

From app/src/main/java/com/horsenma/yourtv/models/TVModel.kt (key methods):
```kotlin
fun getVideoUrl(): String?
fun getMediaItem(): MediaItem?
fun getMediaSource(): MediaSource?
fun setErrInfo(err: String)
var retryTimes: Int
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ExoPlayerCallback Interface and ExoPlayerEngine Class</name>
  <files>
    app/src/main/java/com/horsenma/yourtv/ExoPlayerCallback.kt,
    app/src/main/java/com/horsenma/yourtv/ExoPlayerEngine.kt
  </files>
  <read_first>
    app/src/main/java/com/horsenma/mytv1/WebFragmentCallback.kt,
    app/src/main/java/com/horsenma/yourtv/PlayerFragment.kt,
    app/src/main/java/com/horsenma/mytv1/WebFragment.kt,
    app/src/main/java/com/horsenma/yourtv/SP.kt
  </read_first>
  <action>
    **Create ExoPlayerCallback.kt (per D-08):**
    Package: `com.horsenma.yourtv`
    Interface mirroring WebFragmentCallback pattern with additional ExoPlayer-specific events:
    - `onPlaybackStarted()` — called when playback starts successfully
    - `onPlaybackStopped()` — called when playback stops
    - `onPlaybackError(error: String)` — called on player error
    - `onVideoSizeChanged(width: Int, height: Int)` — called when video dimensions change
    - `onPlaybackStateChanged(state: Int)` — called on ExoPlayer state transitions (BUFFERING, READY, IDLE, ENDED)

    **Create ExoPlayerEngine.kt (per D-06, D-07):**
    Package: `com.horsenma.yourtv`

    Class structure:
    - Private fields: `player: ExoPlayer?`, `callback: ExoPlayerCallback?`, `tvModel: TVModel?`, `handler: Handler`, `context: Context?`, `playerView: PlayerView?`
    - Monitoring fields: `isStable: Boolean`, `playbackStartTime: Long`, `lastStopTime: Long`, `bufferingCount: Int`, `lastSwitchTime: Long`, `checkPlaybackInterval: Long = 15_000L`

    Methods to implement (extracted from PlayerFragment.kt per PATTERNS.md):
    - `create(context: Context, playerView: PlayerView)` — ExoPlayer initialization with renderers factory, PlayerMediaCodecSelector, soft decode support (from PlayerFragment lines 303-326)
    - `setCallback(callback: ExoPlayerCallback)` — register callback
    - `play(tvModel: TVModel)` — start playing a TVModel, set up player listener (from PlayerFragment lines 940-960 IPTV branch, lines 327-628 listeners)
    - `switchSource(tvModel: TVModel)` — switch to different source URL with HLS support (from PlayerFragment lines 854-937)
    - `stop()` — stop current playback without destroying engine
    - `release()` — full cleanup: handler.removeCallbacksAndMessages(null), player.release(), player=null, callback=null, tvModel=null, context=null, playerView=null (per D-09, D-10)
    - `ensurePlaying()` — resume if paused (from PlayerFragment lines 844-851)
    - `getCurrentResolution(): Pair<Int, Int>?` — get current video resolution (from PlayerFragment lines 1222-1231)
    - `isPlaying(): Boolean` — check if player is currently playing
    - `getCurrentTvModel(): TVModel?` — return current tvModel

    Internal methods:
    - `setupPlayerListener()` — create and add Player.Listener with callbacks to ExoPlayerCallback (from PlayerFragment lines 327-628)
    - `startPlaybackMonitor()` — start checkPlaybackRunnable on handler (from PlayerFragment lines 715-778)
    - `stopPlaybackMonitor()` — remove handler callbacks

    Include `PlayerMediaCodecSelector` inner class (from PlayerFragment lines 1179-1220) for API 23 compatibility and soft decode preference.

    Use `@OptIn(UnstableApi::class)` where needed for ExoPlayer APIs.
    Use `private const val TAG = "ExoPlayerEngine"` for logging.
    Follow existing codebase conventions: 4-space indent, Log.d/e/w with TAG, camelCase methods.
  </action>
  <verify>
    <automated>cd D:/Computers/AIDevelop/Tools/mytv && grep -c "interface ExoPlayerCallback" app/src/main/java/com/horsenma/yourtv/ExoPlayerCallback.kt && grep -c "class ExoPlayerEngine" app/src/main/java/com/horsenma/yourtv/ExoPlayerEngine.kt</automated>
  </verify>
  <acceptance_criteria>
    - ExoPlayerCallback.kt exists with 5 callback methods: onPlaybackStarted, onPlaybackStopped, onPlaybackError, onVideoSizeChanged, onPlaybackStateChanged
    - ExoPlayerEngine.kt exists with class ExoPlayerEngine containing: create(), setCallback(), play(), switchSource(), stop(), release(), ensurePlaying(), getCurrentResolution()
    - ExoPlayerEngine.release() calls handler.removeCallbacksAndMessages(null) before player.release()
    - ExoPlayerEngine contains PlayerMediaCodecSelector inner class
    - ExoPlayerEngine uses ExoPlayerCallback for all state notifications (no direct PlayerFragment references)
    - Both files compile (verified by grep for syntax errors or by including in assembleDebug)
  </acceptance_criteria>
  <done>ExoPlayerCallback interface and ExoPlayerEngine class created with full playback lifecycle management</done>
</task>

<task type="auto">
  <name>Task 2: Refactor PlayerFragment to Use ExoPlayerEngine</name>
  <files>
    app/src/main/java/com/horsenma/yourtv/PlayerFragment.kt
  </files>
  <read_first>
    app/src/main/java/com/horsenma/yourtv/PlayerFragment.kt,
    app/src/main/java/com/horsenma/yourtv/ExoPlayerEngine.kt,
    app/src/main/java/com/horsenma/yourtv/ExoPlayerCallback.kt,
    app/src/main/java/com/horsenma/mytv1/WebFragment.kt
  </read_first>
  <action>
    **Refactor PlayerFragment.kt (per D-06, D-07, D-08, D-09, D-10, D-11):**

    **Add engine field:**
    - Add `private var exoPlayerEngine = ExoPlayerEngine()` field
    - Remove `private var player: ExoPlayer? = null` field (line 66 per PATTERNS.md)

    **Remove extracted code (moved to ExoPlayerEngine):**
    - Remove `updatePlayer()` method (lines 302-639) — now in ExoPlayerEngine.create()
    - Remove `switchSource()` method (lines 854-937) — now in ExoPlayerEngine.switchSource()
    - Remove `ensurePlaying()` method (lines 844-851) — now in ExoPlayerEngine
    - Remove `getCurrentResolution()` method (lines 1222-1231) — now in ExoPlayerEngine
    - Remove `PlayerMediaCodecSelector` inner class (lines 1179-1220) — now in ExoPlayerEngine
    - Remove `checkPlaybackRunnable` (lines 715-778) — now in ExoPlayerEngine
    - Remove `stableSourceCheckRunnable` (lines 93-101) — now in ExoPlayerEngine

    **Set up ExoPlayerCallback in initialization:**
    - In the fragment's initialization (e.g., onViewCreated or where player was previously created), set up the callback:
      ```
      exoPlayerEngine.setCallback(object : ExoPlayerCallback {
          onPlaybackStarted: update playback state flags (playbackStartTime, bufferingCount, isStable, etc.)
          onPlaybackStopped: update state flags
          onPlaybackError: update state, set tvModel error info
          onVideoSizeChanged: call updatePlayerViewLayout() if not in PiP
          onPlaybackStateChanged: handle buffering detection if needed
      })
      ```
    - The callback implementations should contain the same state management logic that was previously inline in the Player.Listener callbacks (from PlayerFragment lines 327-628)

    **Refactor play() method (lines 940-1156):**
    - IPTV branch: replace inline ExoPlayer creation with `exoPlayerEngine.create(requireContext(), binding.playerView)` followed by `exoPlayerEngine.play(tvModel)`
    - WebView branch: keep existing WebFragment creation logic, but add `exoPlayerEngine.release()` before switching to WebView (per D-09)

    **Refactor engine switching (per D-09):**
    - IPTV -> WebView: call `exoPlayerEngine.release()`, then hide playerView, show webView, create WebFragment
    - WebView -> IPTV: remove WebFragment via childFragmentManager, then `exoPlayerEngine.create(requireContext(), binding.playerView)` + `exoPlayerEngine.play(tvModel)`
    - Keep the fragment transaction safety guards: `isAdded && !isDetached && !childFragmentManager.isStateSaved`

    **Refactor onDestroyView (per D-10):**
    - Replace `player?.release()` with `exoPlayerEngine.release()`
    - Keep existing: `_binding = null`, `requireActivity().unregisterReceiver(screenReceiver)`

    **Keep in PlayerFragment (NOT extracted):**
    - UI layout management (playerView vs webView visibility toggling)
    - WebView fragment transaction management (childFragmentManager operations)
    - Gesture handling delegation to MainActivity
    - Source button management
    - enterPictureInPictureMode / exitPictureInPictureMode
    - updatePlayerViewLayout() (UI concern)

    **Preserve MainViewModel state (per D-11):**
    - Channel state is already held in MainViewModel LiveData — no changes needed
    - Confirm that tvModel is obtained from viewModel, not from ExoPlayerEngine
  </action>
  <verify>
    <automated>cd D:/Computers/AIDevelop/Tools/mytv && rtk ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PlayerFragment.kt contains `private var exoPlayerEngine = ExoPlayerEngine()` field
    - PlayerFragment.kt does NOT contain `private var player: ExoPlayer?`
    - PlayerFragment.kt does NOT contain `fun updatePlayer()` or `fun switchSource()` as standalone methods
    - PlayerFragment.kt does NOT contain `class PlayerMediaCodecSelector`
    - PlayerFragment.kt does NOT contain `checkPlaybackRunnable` or `stableSourceCheckRunnable` definitions
    - PlayerFragment.kt DOES contain `exoPlayerEngine.create(`, `exoPlayerEngine.play(`, `exoPlayerEngine.release(`
    - PlayerFragment.kt DOES contain `ExoPlayerCallback` implementation
    - PlayerFragment.kt DOES contain fragment transaction safety guards (`isAdded && !isDetached`)
    - Project compiles successfully (assembleDebug exits 0)
  </acceptance_criteria>
  <done>PlayerFragment delegates all ExoPlayer operations to ExoPlayerEngine; engine switching releases previous engine immediately; project compiles</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PlayerFragment <-> ExoPlayerEngine | Callback interface boundary — engine notifies fragment of state changes |
| ExoPlayerEngine <-> Android ExoPlayer | Library API boundary — engine wraps ExoPlayer lifecycle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | ExoPlayerEngine memory leak | mitigate | release() calls handler.removeCallbacksAndMessages(null) before player.release(); all references nulled |
| T-02-02 | Tampering | Fragment transaction crash | mitigate | Guard all childFragmentManager transactions with isAdded/isDetached/isStateSaved checks |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this phase |
</threat_model>

<verification>
1. Build: `./gradlew assembleDebug` exits 0
2. Engine extraction: `grep -c "ExoPlayer" app/src/main/java/com/horsenma/yourtv/PlayerFragment.kt` shows only exoPlayerEngine references, no direct ExoPlayer imports for player management
3. Callback interface: `grep -c "interface ExoPlayerCallback" app/src/main/java/com/horsenma/yourtv/ExoPlayerCallback.kt` returns 1
4. Engine class: `grep -c "class ExoPlayerEngine" app/src/main/java/com/horsenma/yourtv/ExoPlayerEngine.kt` returns 1
5. Handler cleanup: `grep -n "removeCallbacksAndMessages" app/src/main/java/com/horsenma/yourtv/ExoPlayerEngine.kt` shows cleanup in release()
</verification>

<success_criteria>
- ExoPlayerEngine encapsulates all ExoPlayer lifecycle (create, play, switchSource, release)
- ExoPlayerCallback interface provides 5 callback methods
- PlayerFragment has no direct ExoPlayer player management code
- Engine switching immediately destroys previous engine (per D-09)
- Handler callbacks cleared on release (per D-10)
- Channel state preserved via MainViewModel during switches (per D-11)
- Project compiles successfully
</success_criteria>

<output>
Create `.planning/phases/01-verification-bypass-playback-architecture/01-02-SUMMARY.md` when done
</output>
